import React from 'react';
import './User.css';

// creates User component to render to the page
const User = () => {
    return (
        <div>User</div>
    )
};

// exports User for external use
export default User;
